import { Syne } from "@next/font/google";

export const syneBody = Syne({ subsets: ["latin"], weight: "500" });
export const syneHeading = Syne({ subsets: ["latin"], weight: "700" });
export const syneHeadingBoldest = Syne({ subsets: ["latin"], weight: "800" });
